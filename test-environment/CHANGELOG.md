# Test Environment Escalation Changelog

سجل رسمي لكل جولة تصعيد في صعوبة بيئة الاختبار — راجع
`docs/REQUIREMENTS.md` قسم 8 ("Escalation Cycle") للدورة الكاملة
والمبدأ اللي بيحكم القراءة الصحيحة لأي سطر هنا.

> **مبدأ أساسي (منقول حرفيًا من docs/REQUIREMENTS.md قسم 8):** "كل
> الاختبارات نجحت" في أي جولة تعني الكود قوي بما يكفي للمستوى الحالي
> من الصعوبة بس - مش دليل إن الكود جاهز للواقع، ومش وصلنا للحد
> الأقصى. السقف الحقيقي يفضل مجهول طالما بيئة الاختبار لسه بتتطور.

هذا الملف يبدأ فارغًا اعتبارًا من 2026-08-21 — أول جولة تصعيد رسمية
بعد إنشاء الدورة تُسجَّل هنا. تاريخ توسعة الـ Test Targets قبل كده
(المستويات 1-5، Tier 2، إصلاحات `render_wait_ms`/`click_selector`)
موثّق بالتفصيل في `docs/REQUIREMENTS.md` أقسام 6 و7 — مش معاد تسجيله
هنا بنفس الصيغة رجعيًا، عشان الترقيم بالشكل `X → X+1` يبدأ من نقطة
واضحة بدل ما يتفرض على تاريخ مش متسجل أصلاً بهذا الشكل.

## الصيغة الثابتة لكل سطر

```
[التاريخ] من مستوى X لمستوى X+1 - أضفنا كذا - نتيجة الكود (نجح كامل/فشل جزئي/فشل كامل) - القرار (نصلح دلوقتي/نأجل ونسجل/نقبله كحد)
```

- **نتيجة الكود**: واحدة من الثلاثة بالظبط — نجح كامل / فشل جزئي / فشل كامل
- **القرار**: واحد من الثلاثة بالظبط — نصلح دلوقتي / نأجل ونسجل / نقبله كحد
- تفاصيل أي فجوة مكتشفة (السبب الجذري، الحل المحتمل، الكود اللي
  اتضاف) تتوثّق كاملة في `docs/REQUIREMENTS.md` (قسم 6 لتغطية Test
  Targets، قسم 7 لـ Known Spider Limitations) — السطر هنا إشارة/فهرس
  بس، مش تكرار.

## السجل

<!-- كل جولة تصعيد جديدة تتضاف كسطر جديد فوق هنا، الأحدث أولاً. -->

[2026-08-26] **تصعيد في الأداة (مش في البيئة) — إصلاح race condition حقيقي، مش تصعيد صعوبة جديد** (تحقيق مستقل طلبه المستخدم صراحة أثناء وقف مؤقت لـ JA4 experiment branch) - عائلة اختبار الـprogressive DOM Virtualization (بند 14) كانت بتفشل بشكل متكرر عبر 7 محاولات CI منفصلة (أرقام مختلفة كل مرة: 21/20/0/24 من المتوقع 25، دايمًا أقل، أبدًا نفس الرقم الثابت) - **السبب الجذري الحقيقي (اتأكّد من قراءة `templates/feed.html` مباشرة):** `loadMore()`'s `loading` flag بيتجاهل بصمت أي نداء يوصل والـfetch السابق لسه شغّال، و`scroll_and_collect`'s الـpause_ms الثابت (`_scroll.py`) كان تخمين لمدة "fetch+render+trim كاملة" مش إشارة انتهاء حقيقية - تحت ضغط CI متغيّر، ده بينتج race عشوائي (رقم ناقص عشوائي)، مش نقص ثابت - **مختلف تمامًا عن حالة الـ"25→0" (كراش متصفح حقيقي، `Page.wait_for_timeout: ... browser has been closed`) اللي اتفرّقت عنه صراحة زي ما طلب المستخدم** - **الإصلاح:** `settle_fn` parameter اختياري جديد (افتراضي `None`، صفر تغيير سلوك قديم) في `scroll_and_collect`/`collect_html_snapshots`/`collect_live_dom_items_progressively` بينادى بعد الـscroll+dispatch وقبل الـpause_ms القديم - `_scroll.py`/`_live_dom.py` فضلوا من غير أي استيراد playwright/patchright (نفس مبدأ الـ`Any`-typing) - كل provider بيبني `settle_fn` بتاعه بـ`page.wait_for_load_state("networkidle", timeout=5000)` ماسك الـtimeout الخاص بمكتبته هو (`playwright.sync_api.TimeoutError`/`patchright.sync_api.TimeoutError` - مش نفس الكلاس) - نتيجة الكود محليًا: ruff/mypy --strict نظيفين، 299 unit+contract test PASSED (85.48% coverage، فوق الـgate) - **لسه محتاجين تأكيد CI حقيقي عبر عدة محاولات** (مش محاولة واحدة، بما إن الـrace كانت عشوائية أصلًا) - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 17 - القرار: نصلح دلوقتي (نتيجة الـCI هتتسجّل هنا بمجرد ما الـrun(s) تخلص) - **❌ تحديث لاحق نفس اليوم — المحاولة الأولى فشلت فعليًا:** CI run [32973393111](https://github.com/malekazmy00/TITAN-APEX/actions/runs/32973393111) (commit `9199905`) فشل بنفس الرقم بالظبط (20 من 25) اللي كان بيفشل بيه قبل الإصلاح خالص - السبب: `page.wait_for_load_state("networkidle")` عبارة عن flag خاص بدورة حياة الـnavigation، بيرجع فورًا بعد أول مرة يوصله (بدري جدًا)، فكل الـsettle_fn calls اللي بعد كده كانت no-op بالكامل - أداة غلط تمامًا لمزامنة fetch متكرر على نفس الصفحة - **الإصلاح المُصحَّح:** الكود بقى بيتابع الطلبات الشبكية بنفسه مباشرة (`page.on("request"/"requestfinished"/"requestfailed")`) بدل الاعتماد على فلاج Playwright المُخزَّن - `poll_until_idle` + `RequestCounter` (الاتنين دوال/classes نقية بالكامل في `_scroll.py`، مُختبَرين بالكامل بدون أي متصفح) بيفصلوا منطق التوقيت/العداد عن الـwiring اللي محتاج `Page` حقيقي - سطر اللوج النهائي كسب `network_idle_timeouts` field كدليل تشخيصي لأي CI run جاي - نتيجة الكود محليًا بعد الإصلاح المُصحَّح: 309 unit+contract test PASSED (85.13% coverage) - **لسه محتاجين تأكيد CI حقيقي للإصلاح المُصحَّح** - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 17 - **❌ تحديث لاحق نفس اليوم — غلطة عملية في التحقق المحلي، مش في الإصلاح نفسه:** CI run [32977436823](https://github.com/malekazmy00/TITAN-APEX/actions/runs/32977436823) (commit `523da12`) فشل في خطوة الـunit tests + coverage gate **قبل ما يوصل لاختبارات الـDOM Virtualization الحية خالص** - السبب: CI بيشغّل `pytest tests/unit --cov=src --cov-fail-under=85` **لوحده**، لكن التحقق المحلي كان `tests/unit tests/contract` مع بعض (رقم متفائل زيادة، 85.13% بدل 84.89% الحقيقي) - الكود نفسه سليم، الفجوة كانت في عدد اختبارات unit لوحدها بعد ما الكود الجديد غير القابل للاختبار بدون متصفح كبّر - اتضافت اختبارات حقيقية جديدة لفجوات تغطية موجودة من الأول ومالهاش علاقة بالتحقيق ده (`byparr_provider.py`'s `login_flow_unsupported`/`progressive_extraction_unsupported`، و`_live_dom.py`'s unrecognized-pseudo-after-separator) - اتّحقّق محليًا بنفس أمر CI بالحرف: 283 test PASSED، 85.26% (هامش حقيقي فوق الـgate) - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 17

[2026-08-24] **تصعيد مركّب: بيئة + أداة مع بعض** (محور 6 "محتوى غير متوقع" من `docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md`: Interstitials، طلب المستخدم صراحة بعد Login/Session) - أضفنا `structural/interstitial.py` (`render_interstitial_script` لـ`"time"`/`"scroll"` triggers، `build_interstitial_feed_page` content generator خاص) + مسار جديد معزول `/feed-interstitial` + `/api/feed-interstitial` (`app.py`) - `/` و`/feed` الأصليين اتسابوا من غير أي تعديل - **قرار تصميمي حقيقي قبل كتابة أي كود:** الحجب بيتم عبر JS flag (`window.__interstitialShown`) بيتفحص جوّه `loadMore()`، مش `overflow: hidden` على الـ body - السبب: `_scroll.py` نفسه موثّق فيه إن الحلقة بتعمل `dispatchEvent` بغض النظر عن الموضع الفعلي، فحجب CSS بس مكانش هيوقف الـ synthetic dispatch فعليًا - **الاتنين (time/scroll) مُنفّذين بالكامل وقابلين للتهيئة**، بس الـ live CI شغّالة بوضع `"time"` بس (حتمي)؛ وضع `"scroll"` مش هيتـ-live-CI-اختبر end-to-end الجولة دي - **قيد معماري حقيقي اتسجّل صراحة:** آلية `click_selector` الحالية بتضغط قبل أي scroll خالص، فمينفعش تقفل interstitial بيظهر بس أثناء scroll فعلي - ده بند منفصل لو احتجناه لاحقًا، مش حل ناقص - الكود اتجرّب من غير تعديل الأول (`mock_target_interstitial_camoufox_unhandled.yaml`، بدون click_selector) قبل أي fix، زي ما طُلب - الحل المتوقع: نفس آلية cookie wall بالحرف (`click_selector: '[data-role="interstitial-close"]'`) - 3 configs جديدة (unhandled + camoufox/patchright dismissed) - نتيجة الكود محليًا: ruff/mypy --strict نظيفين، نفس أوامر CI بالظبط (264 unit passed 86.08%، 29 contract passed، مفيش تغيير لأن مفيش كود src/ جديد)، test-environment's own 158 unit test PASSED (100% coverage) - **لسه محتاجين تأكيد CI حقيقي** للاختبارات الحية الثلاثة الجديدة - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 16 - القرار: نصلح دلوقتي (نتيجة الـ CI هتتسجّل هنا بمجرد ما الـ run يخلص) - **❌ تحديث لاحق نفس اليوم:** CI run 32789920874 (commit `cb42c10`) فشل فعليًا في الاختبارات الحية الـ2 اللي فيهم click_selector - السبب الجذري الحقيقي (2 حاجة مختلفة): (1) `loadMore()` مربوطة بحدث `'scroll'` حقيقي (native)، وبـ5 posts قصيرة بس الصفحة مكانتش طويلة كفاية تعمل scroll فعلي - يعني `click_selector` مكانش الفاشل، كانت الآلية نفسها بتتوقف بغض النظر عن الـ interstitial - الحل: `progressive_extraction: true` (نفس حل بند 14 لـ DOM Virtualization، `dispatchEvent` صناعي إجباري كل محاولة) - (2) Patchright: مش فجوة جديدة خالص - رجعنا للتوثيق ولقينا PatchrightProvider موثّق بالفعل إنه مرفوض من Anubis قبل `load` على كل route، مش بس `/` - غلطة عملية مني إني ماراجعتش ده قبل ما أكتب الـ config - الـ config اتحول لتأكيد القيد الموجود (0 items متوقعة بدل 15) مش اختبار fix جديد - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 16 - **✅✅ تحديث نهائي نفس اليوم:** CI run 32849689626 (commit `b0c1c62`) نجح بالكامل - 37 integration test PASSED (0 failed) - الاختبارات الحية الـ3 نجحت بالاسم بالأرقام الصحيحة المُصحَّحة: unhandled=5 items، camoufox dismissed=15 items، patchright=0 items (Anubis logs نفسها أكّدت "explicit deny" بقاعدة bot/headless-chrome، بالظبط زي المتوقع) - بند Interstitials اتحل بالكامل بدليل CI حقيقي

[2026-08-24] **تصعيد مركّب: بيئة + أداة مع بعض** (محور 5 "المصادقة والجلسات" من `docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md`: Login/Session، Known Limitation #1، طلب المستخدم صراحة قبل أي بند تاني من الجدول) - أضفنا `security/auth.py` (`CsrfTokenStore` single-use، `SessionStore` TTL حقيقي + injectable clock، `check_credentials` بـ`hmac.compare_digest`) + 3 routes جديدة (`GET/POST /login`، `GET /feed-protected` — 401 صريح من غير session، `GET /test-expire-session` instrumentation اختباري) - `/` و`/feed` الأصليين اتسابوا من غير أي تعديل - **فجوة معمارية حقيقية اتكشفت قبل كتابة أي كود تنفيذي:** كل route (بما فيهم الجداد) وراء Anubis (اتأكّد من `botPolicy.yaml` نفسه، مفيش path exemption) + كل نداء `AntibotProvider.solve()` بيشغّل متصفح جديد ويقفله (كوكيز مبتفضلش بين نداءين) - الحل: رحلة login كاملة جوّه نداء solve() واحد بس، مش عبر Scrapy requests منفصلة - أضفنا `LoginFlow` model + `solve()` كسب `login_flow` (best-effort) + `src/providers/antibot/_login.py` (`submit_login_form`/`perform_login_and_navigate`) + `GenericSpider`'s `handle_httpstatus_list: [401, 403]` غير مشروط (فجوة تانية: Scrapy's HttpErrorMiddleware بيرمي أي 401/403 قبل `parse()` من غير الـ opt-in ده) - نتيجة الكود محليًا: ruff/mypy --strict نظيفين، 289 unit+contract test PASSED (85.25% coverage)، test-environment's own 137 unit test PASSED (100% coverage) - 3 configs جديدة (`mock_target_login_protected.yaml`/`_session_expiry.yaml`/`mock_target_feed_protected_no_login.yaml`) - **لسه محتاجين تأكيد CI حقيقي** للاختبارات الحية الثلاثة الجديدة - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 15 - القرار: نصلح دلوقتي (نتيجة الـ CI هتتسجّل هنا بمجرد ما الـ run يخلص) - **✅ تحديث لاحق نفس اليوم:** أول push فشل فعليًا (CI run 32771322702) في خطوة `Unit tests (coverage gate >= 85%)` بس (260 passed، 85.00% بالظبط — عند الحد مش فوقه)؛ السبب الجذري الحقيقي كان في عملية التحقّق المحلي نفسها (بتحسب `tests/unit + tests/contract` مع بعض، بينما CI بيشغّل `tests/unit` **بس** كخطوة منفصلة تمامًا) - اتصلح باستخراج منطق إضافي قابل للاختبار (`log_login_outcome` في `_login.py`) وأعيد التحقّق بنفس أوامر CI بالظبط محليًا (264 passed، 86.08%) - CI run 32785461995 (commit `e49c745`) نجح بالكامل: coverage gate 86.08%/264 passed، contract tests 29 passed، test-environment 137 passed (100%)، **والاختبارات الحية الثلاثة الجديدة نجحت بالاسم فعليًا** (`test_login_flow_reaches_protected_data_after_a_real_post_and_csrf_token`، `test_feed_protected_without_any_login_yields_nothing_not_a_crash`، `test_session_expired_mid_crawl_after_a_real_login_yields_nothing_not_a_crash`) - ملاحظة: أول محاولة تشغيل لنفس الـ commit فشلت في اختبار Shadow DOM قديم غير مرتبط (`test_mock_target_live_dom_recovers_every_shadow_dom_wrapped_post`، كراش حقيقي في المتصفح `Target crashed`)، اتأكّد إنه flake بإعادة تشغيل الـ job الفاشل بس ونجح فعليًا في المحاولة التانية - Lint run 32785461981 نجح كمان

[2026-08-24] **تصعيد في الأداة (مش في البيئة)** — حل DOM Virtualization فعليًا (بند 13's Known Limitation) بـ progressive scroll + incremental extraction، طلب المستخدم صراحة بعد بحث في أحدث الممارسات الموثّقة (2026) - أضفنا `SpiderConfig.progressive_extraction: bool` (افتراضي false) - `scroll_and_collect`/`collect_html_snapshots` (`_scroll.py`) و`collect_live_dom_items_progressively` (`_live_dom.py`) بيلقّطوا/يستخرجوا بعد **كل** خطوة scroll لوحدها (قبل ما eviction يشيل اللي معروض)، مش قراءة نهائية واحدة، ودي مجمّعة بـ`post_id` كمفتاح dedup عبر الـ crawl **كله** - `parsed_html.py` وحدة جديدة تفكّك كل HTML snapshot عبر `parsel.Selector` مباشرة، `generic_spider.py` بيعمل merge/dedupe بنفسه - configs جديدة (`*_progressive_parsed_html.yaml`/`_live_dom.yaml`)، بند 13's القديمة فضلت كـ regression sentinel من غير تعديل - **3 محاولات موثّقة بالكامل قبل النجاح (زي نمط JSON parsing بالظبط):** محاولة 1 (توقع 10) فشلت فعليًا (CI run 32730994089: got 5 مش 10) - السبب: `scroll_and_collect` وارث heuristic "قف لو `scrollHeight` بطل يكبر" من الكود القديم، غلط لهدف virtualized (الارتفاع بيفضل ثابت بسبب الـ eviction نفسه) - محاولة 2 (مراجعة: شيل الاعتماد على `scrollHeight` خالص + `dispatchEvent(new Event('scroll'))` صريح، وإعادة اشتقاق التوقع لـ25) فشلت جزئيًا (CI run 32733064348 بعد ما اتأكدت flake حقيقية غير متعلقة في المحاولة الأولى لنفس الـ commit عبر `rerun_failed_jobs`: `live_dom` رجع 25 بالظبط، بس `parsed_html` رجع 20 - race condition حقيقي في `loading` flag بتاع `loadMore()` مع الـ 700ms pause المشترك) - محاولة 3 (ثوابت أسخى مخصّصة للـ progressive path بس: 10 محاولات، 1500ms pause) - **✅✅✅ اتأكّدت فعليًا** (CI run [32735734451](https://github.com/malekazmy00/TITAN-APEX/actions/runs/32735734451): `31 passed, 0 failed` - `test_progressive_parsed_html_recovers_every_virtualization_window` و`test_progressive_live_dom_recovers_every_virtualization_window` الاتنين PASSED صراحة بالرقم الحتمي المتوقع (25 بالظبط)، وبند 13's sentinels القديمة لسه PASSED بـ5 بالظبط (regression check كامل) - 262 unit+contract test PASSED محليًا (86.05% coverage)) - تفاصيل كاملة للثلاث محاولات + الاشتقاق الرياضي الكامل (ليه 25 مش 10) في `docs/REQUIREMENTS.md` قسم 9 بند 14 و`tests/integration/test_mock_target_dom_virtualization_progressive_live.py`'s docstring - القرار: نصلح دلوقتي

[2026-08-24] من مستوى 9 لمستوى 10 (تصعيد **في البيئة والأداة مع بعض** — محور 3 "البنية الهيكلية" من `docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md`: DOM Virtualization) - أضفنا `structural/dom_virtualization.py` (`excess_count`) + `templates/feed.html` بيشيل أقدم البوستات فعليًا من الـ DOM (`removeChild`) لما العدد يعدّي `DOM_VIRTUALIZATION_WINDOW_SIZE` (افتراضي 5) - **فجوة تمهيدية اتكشفت في الطريق:** `CamoufoxProvider`/`PatchrightProvider` ما كانش عندهم أي قدرة scroll خالص - `PlaywrightMiddleware`'s scroll loop مستحيل معماريًا يوصله أي target وراء Anubis - اتصلحت بوحدة مشتركة جديدة `src/providers/antibot/_scroll.py` - أضفنا config جديدين (`mock_target_feed_virtualized_parsed_html.yaml`/`_live_dom.yaml`) لاختبار الاتنين ضد `/feed` - نتيجة الكود محليًا: test-environment's own 101 unit test PASSED (100% coverage)، 230 unit+contract test PASSED على `src` (88.64%) - التوقع الحتمي: `len(items) == 5` من الاتنين (parsed_html وlive_dom) - **✅✅✅ الفرضية اتأكّدت فعليًا** (CI run [32725098955](https://github.com/malekazmy00/TITAN-APEX/actions/runs/32725098955): `29 passed, 0 failed` - `test_parsed_html_only_recovers_the_final_virtualization_window` و`test_live_dom_also_only_recovers_the_final_virtualization_window` الاتنين PASSED صراحة بالرقم الحتمي المتوقع (5 بالظبط) - يعني `extraction_mode: live_dom` (اللي حل Shadow DOM في الجولة اللي فاتت) ماقدرش يحل virtualization - نفس السبب: الاتنين بيقروا حالة الـ DOM في نفس اللحظة، بعد ما الـ scroll loop يخلص - باقي الـ 27 اختبار تاني كلهم PASSED برضه، وtest-environment's own 101 unit test PASSED) - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 13 - القرار: نقبله كحد (فجوة معمارية حقيقية مختلفة النوع عن Shadow DOM، مش قابلة للحل بدون تجميع تدريجي أثناء الـ scroll بدل قراءة نهائية واحدة - خارج نطاق الجولة دي عن قصد)

[2026-08-24] **تصعيد في الأداة (مش في البيئة)** — حل Shadow DOM فعليًا بتعديل معماري في `src/`، من غير أي مكتبة خارجية (طلب المستخدم صراحة، ردًا على بند 9's Known Limitation) - أضفنا `SpiderConfig.extraction_mode: "parsed_html" | "live_dom"` (افتراضي القديم) - لما `live_dom`، `CamoufoxProvider`/`PatchrightProvider` بيستخرجوا الـ items مباشرة من الصفحة الحية (`page.locator()`) قبل ما المتصفح يتقفل، مستغلين اختراق Playwright's التلقائي لـ shadow roots المفتوحة - `AntibotProvider.solve()` كسب `extraction_selectors` (best-effort زي `click_selector`)، `Solution.items` جديد، وحدة مشتركة `src/providers/antibot/_live_dom.py` بتعيد استخدام نفس صيغة selectors القديمة (`::text`/`::attr()`) - config جديد `mock_target_live_dom.yaml` (نفس target `mock_target_camoufox.yaml` بالظبط، بس extraction_mode مختلف - القديم اتسيب من غير تغيير عمدًا، لسه بيوثّق الفجوة) - نتيجة الكود محليًا: 224 unit+contract test PASSED (88.52% coverage)، test-environment's own 92 unit test PASSED (100% coverage) - **تصحيح حقيقي لتوقّع الطلب الأصلي:** decoy twin بيتلقط برضه مع locator بيخترق shadow DOM، فالعدد الحتمي الصح 11 مش 10 (زي الـ baseline قبل Shadow DOM أصلاً) - **✅✅✅ اتأكّد فعليًا** (CI run [32680454673](https://github.com/malekazmy00/TITAN-APEX/actions/runs/32680454673): `27 passed, 0 failed` - `test_mock_target_live_dom_recovers_every_shadow_dom_wrapped_post` PASSED صراحة بالرقم الحتمي المتوقع (11 بالظبط)، وفي نفس الوقت `mock_target_camoufox.yaml` (المتروك من غير تعديل) لسه بيرجّع 6 بالظبط - الفجوة التاريخية (بند 11) اتوثّقت ومحفوظة، والحل (بند 12) بيضيف عليها مش بيمسحها - test-environment's own 92 unit test PASSED برضه) - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 12 - القرار: نصلح دلوقتي

[2026-08-24] من مستوى 8 لمستوى 9 (تصعيد **في البيئة** — محور 3 "البنية الهيكلية" من `docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md`: Shadow DOM، العنصر الخامس والأخير في السيناريو المركّب، فوق نفس الأربعة النشطين من الجولة اللي فاتت) - أضفنا `structural/shadow_dom.py` في `test-environment/mock-target` - نص البوستات (index فردي، 0-based) بيترندر جوه shadow root حقيقي بيتلحق client-side (`attachShadow`)، مش أي حيلة `display:none` زي باقي الطبقات - الـ payload بيتشفّر base64(JSON) في `data-shadow-payload`، وسكريبت client-side بيبنيه فعليًا في shadow root حقيقي - نتيجة الكود: **Known Limitation حقيقية متوقعة** (مش فجوة توقيت زي باقي الفجوات اللي اتصلحت - فجوة معمارية: shadow root attached عبر `attachShadow()` مابيتضمّنش خالص في `outerHTML` serialization حسب DOM spec نفسه، فـ`page.content()` (اللي كل الـ 3 providers بيرجّعوه) وGenericSpider's string-based CSS selector مايوصلوش له حتى عبر متصفح حقيقي كامل عدّى كل طبقة تانية على نفس الصفحة) - دليل حتمي قابل للتكرار: عدد items المتوقع = 6 بالظبط (5 بوست حقيقي في light DOM + decoy واحد)، مش 10 - test-environment's own pytest suite: 92 passed، 100% coverage محليًا قبل الدفع - **✅✅✅ اتأكّد فعليًا** (CI run [32678444498](https://github.com/malekazmy00/TITAN-APEX/actions/runs/32678444498): `26 passed, 0 failed` - الاختبار الجديد نفسه (`test_mock_target_camoufox_misses_every_shadow_dom_wrapped_post`) PASSED صراحة بالرقم الحتمي المتوقع (6 items بالظبط)، وباقي الـ 25 اختبار PASSED برضه - regression check كامل) - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 11 - القرار: نأجل ونسجل (فجوة معمارية حقيقية ومؤكدة الآن بدليل CI، مش تعديل سريع زي فجوات التوقيت اللي اتصلحت قبل كده)

[2026-08-24] **تصعيد تكامل، مش تصعيد صعوبة** (docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md، "العقبات المركّبة" — أول نص فيها بالحرف: "دفاع طبقة واحدة سهل تجاوزه") - **مفيش كود إنتاجي جديد ولا طبقة عقبة جديدة** - الأربعة طبقات القديمة المُختبرة (honeypots + markup randomizer من Round 1، cookie wall + A/B variants من الجولات اللي بعدها) كانت أصلاً شغّالة مع بعض بالصدفة في كل CI run من Round 6 وما بعده (`docker-compose.test.yml`'s `ENABLE_*` flags كلها افتراضها `true`، ومفيش جولة قفلت طبقة قديمة عشان تختبر طبقة جديدة لوحدها) - بس مفيش اختبار واحد كان بيتحقق من دليل مباشر إن الأربعة اتعدّوا/اتجنّبوا **مع بعض** في نفس الطلب، كل الاختبارات كانت بتتأكد من النتيجة المجمّعة بس (`items > 0`) - أضفنا اختبار جديد (`test_mock_target_camoufox_crawl_gets_real_posts_and_never_reaches_a_real_honeypot`) بيتحقق من حجم `honeypot_triggers.log` قبل/بعد نفس الكراول اللي بيتأكد إنه رجع `items > 0`، يعني بيثبت الأربعة أدلة مع بعض من نفس الطلب الحقيقي (Camoufox بس - Byparr/Patchright عندهم نفس نوع الاختبار أصلاً بس تافه ليهم لأنهم أصلاً بيتوقفوا عند Anubis قبل ما يوصلوا لمحتوى حقيقي) - نتيجة الكود: **✅ نجح كامل، مؤكد فعليًا** (CI run [32677298333](https://github.com/malekazmy00/TITAN-APEX/actions/runs/32677298333): `25 passed, 0 failed` - الاختبار الجديد نفسه PASSED صراحة، وباقي الـ 24 اختبار (byparr/patchright sentinels، JSON API، Test Targets التانية، test-environment's 81 unit test) كلهم PASSED برضه - regression check كامل) - القرار: نصلح دلوقتي - تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 10 - **الخطوة الجاية (شرطها اتحقق فعليًا، بدأنا فيها):** إضافة Shadow DOM كطبقة خامسة، جولة تصعيد صعوبة حقيقية هذه المرة، فوق نفس الأربعة النشطين - جولة منفصلة (Round تانية) هتوثّق لوحدها، عشان أي فشل فيها نعرف يقين إنه بسبب Shadow DOM تحديدًا مش تفاعل غامض مع الأربعة القدام

[2026-08-23] من مستوى 7 لمستوى 8 (تصعيد **في الأداة** — بند 4 من `docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md`: JSON/API parsing) - أضفنا `response_format: "html" | "json"` + `json_selectors` (dotted-key paths + pagination) في `SpiderConfig`، و`GenericSpider._parse_json` جديد (يستخدم `response.json()`، `_resolve_json_path`، `_build_next_json_url`) + `mock_target_feed.yaml` أول target حقيقي يشاور على `/api/feed` (اللي اتبنى من الجولة الأولى بس ما كانش متوصّل) - نتيجة الكود: **فشل جزئي بمعلومة قيّمة جدًا** (0 items؛ Camoufox عدّى Anubis فعليًا بنجاح (status 200)، بس الـ JSON اتلف قبل ما يوصل لـ `GenericSpider` - السبب الجذري اتأكّد بالدليل (diagnostic logging لأول 300 حرف من الـ body الفعلي، commit `b2ef256`): Firefox (اللي Camoufox بيشغّله) بيلف أي استجابة `application/json` بغلاف HTML خاص بيه (`<html><body><pre>...plaintext.css`) قبل ما `page.content()` يتقرا - كود الـ parsing نفسه اشتغل صح 100%، الفجوة في مصدر البيانات مش في المنطق. مؤكد من CI run 32656904590) - القرار وقتها: نأجل ونسجل - **✅✅✅ تحديث نهائي: اتصلح فعليًا ومؤكد بدليل CI حقيقي بعد 3 محاولات (مش محاولة واحدة)** - محاولة 1 (`response.text()` من أول استجابة `goto()`) فشلت (CI run 32660273266 — الاستجابة الأولى كانت صفحة تحدي Anubis المؤقتة، مش الهدف النهائي) - محاولة 2 (تتبّع **كل** استجابات main-frame وأخذ الأخيرة) صلّحت الـ JSON بس كشفت regression حقيقي تاني (CI run 32662253990 — فلتر الـ frame وحده كان بيلخبط بين navigation حقيقي وطلبات fetch/XHR بتعملها JS الصفحة زي Anubis's `pass-challenge` API) - محاولة 3 (إضافة `resp.request.is_navigation_request()`) صلّحت الاتنين مع بعض، **مؤكدة بالكامل من CI run [32664864782](https://github.com/malekazmy00/TITAN-APEX/actions/runs/32664864782): 24 passed, 0 failed** (الاختبارين الحرجين - JSON API والـ cookie wall regression - اتأكّدوا PASSED صراحة، وباقي الـ 22 اختبار كمان PASSED) - تفاصيل كاملة للثلاث محاولات + النتيجة النهائية في `docs/REQUIREMENTS.md` قسم 9 بند 9

[2026-08-21] من مستوى 6 لمستوى 7 (تصعيد **في البيئة** — بند 3 من `docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md`: placeholder/loading content leakage) - أضفنا `structural/placeholder_content.py` في `test-environment/mock-target` - نص كل بوست الحقيقي بيتعرض كـ placeholder حرفي "Loading..." من طرف السيرفر، والنص الحقيقي متخبّي في `data-real-text`، وسكريبت JS بيبدّله بعد `PLACEHOLDER_DELAY_MS` (افتراضي 500ms) - نتيجة الكود: **نجح كامل** (كل الـ providers الحالية اللي بتعدّي Anubis فعليًا عندها `post_load_wait_ms` افتراضي 5000ms - أكبر بكتير من الـ 500ms - فمفيش تسريب فعلي حصل؛ الطبقة دي بقت regression check دائم لو أي انتظار قصّر مستقبلًا) - مؤكد من CI run 32529748679 - القرار: نصلح دلوقتي (تفاصيل في `docs/REQUIREMENTS.md`، `test-environment/README.md` قسم 2.7)

[2026-08-21] من مستوى 5 لمستوى 6 (تصعيد **مركّب: بيئة + كشف bug حقيقي في الكود** — بند 2 من `docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md`: A/B structural variants) - أضفنا `structural/ab_variant.py` في `test-environment/mock-target` - على كل طلب لـ `/` (مش مربوط بالـ session)، الـ container tag بتاع كل بوست بيتغيّر عشوائيًا بين `<article>` و`<div>` (نفس الـ `data-role`/المحتوى) - نتيجة الكود: **كشفت فعليًا bug حقيقي كان موجود من زمان** - الـ selector `item: 'article[data-role="post"]'` في الـ 3 `mock_target*.yaml` كان tag-qualified، فكان هيفشل نص المرات مع variant "b" - اتصلح فورًا لـ `[data-role="post"]` (attribute-only) **قبل** ما يتسجّل أي فشل حقيقي في CI (اتلاقى بالفحص المباشر للكود، مش بالصدفة من تجربة فاشلة) - مؤكد من CI run 32529349297، كل الاختبارات نجحت كامل بعد الإصلاح - القرار: نصلح دلوقتي (تفاصيل في `docs/REQUIREMENTS.md`، `test-environment/README.md` قسم 2.6)

[2026-08-21] من مستوى 4 لمستوى 5 (تصعيد **مركّب: بيئة + أداة مع بعض** — أول بند من `docs/OBSTACLE_MAP_AND_ESCALATION_SCHEDULE.md`: cookie consent wall) - أضفنا (1) `click_selector` اختياري في عقد `AntibotProvider.solve()` نفسه (best-effort - Camoufox/Patchright بيعرفوا click حقيقي، Byparr بيسجّل تحذير واضح ومكمّل من غيره لأن `/v1` API بتاعه مفيهوش قدرة تفاعل خالص) + `ByparrMiddleware` بيمرّر `request.meta["click_selector"]` تلقائيًا و(2) cookie-consent wall حقيقي في `test-environment/mock-target` (`structural/cookie_wall.py` - محتوى غايب فعليًا من الاستجابة، مش overlay بـ CSS، لحد ما consent cookie تتحط عبر رابط Accept حقيقي) + الـ 3 `mock_target*.yaml` اتحدّثوا بـ `click_selector: "#accept-cookies"` - نتيجة الكود: **نجح كامل** لـ Camoufox (بيعدّي الاتنين مع بعض: Anubis ثم cookie wall، بـ click حقيقي على `#accept-cookies`) - Byparr وPatchright فضلوا صفر items **زي المتوقع بالظبط** لأنهم بيفشلوا أصلاً في مرحلة Anubis الأسبق قبل ما يوصلوا لـ cookie wall خالص (مش معلومة جديدة، موثّق صراحة إنه غير قابل للتمييز عن فجوتهم الأسبق) - مؤكد من CI run 32528886186، 23/23 اختبار PASSED - القرار: نصلح دلوقتي (تفاصيل كاملة في `docs/REQUIREMENTS.md` قسم 9 بند 8)

[2026-08-21] من مستوى 3 لمستوى 4 (تصعيد **في الأداة، مش في البيئة** — إضافة `PatchrightProvider` كخيار تالت أخف لـ `AntibotProvider`) - أضفنا `PatchrightProvider` implementation تالت (نفس معمارية `CamoufoxProvider`، بس Chromium + Patchright's stealth layer فوق Playwright الموجود بدل Firefox) + `antibot_provider: "patchright"` في `SpiderConfig` + contract/unit tests + `mock_target_patchright.yaml` وتشغيله فعليًا ضد نفس تحدي Anubis - نتيجة الكود: **فشل جزئي بمعلومة جديدة قيّمة** (0 items، بس السبب مختلف تمامًا عن فجوة Byparr: Anubis's `bot/headless-chrome` fingerprint rule بيرفض Patchright صراحة (`"msg":"explicit deny"`, `rule:"DENY"`) قبل حتى مرحلة تحدي الـ proof-of-work — يعني `post_load_wait_ms` (سبب وجود Patchright) معندوش فرصة يأثّر خالص هنا، بعكس فجوة Byparr التوقيتية. مؤكد من CI run 32524934383 (21/22 اختبار PASSED، الفاشل الوحيد هو اختبار Patchright نفسه لأنه وقتها كان لسه بيفترض النجاح؛ الاختبار اتحوّل بعدها لـ regression sentinel يوثّق النتيجة الحقيقية (صفر items) زي نمط Byparr بالظبط - **تأكيد نهائي فعلي في CI run 32527047011، 22/22 اختبار PASSED**)) - القرار: نقبله كحد (فجوة fingerprint-based، مش قابلة للحل بمجرد إضافة انتظار زي فجوة Byparr التوقيتية؛ Camoufox already نجح فعليًا لنفس نوع التحدي ده، فمفيش داعي لمزود رابع (nodriver) بناءً على شرط المستخدم الصريح: "لو الاتنين فشلوا" - الاتنين مفشلوش، Camoufox نجح). تفاصيل كاملة + جدول مقارنة محدّث للـ 3 providers في `docs/REQUIREMENTS.md` قسم 9 بند 7 و"Antibot Provider Comparison"

[2026-08-21] من مستوى 2 لمستوى 3 (تصعيد **في الأداة، مش في البيئة** — أول مرة نضيف AntibotProvider تاني بدل ما نزوّد صعوبة mock-target نفسه) - أضفنا `CamoufoxProvider` implementation تاني لـ `AntibotProvider` (يشغّل Camoufox حقيقي in-process، بيتحكم بنفسه في توقيت إغلاق المتصفح عبر `post_load_wait_ms` قابل للتهيئة) + حقل `antibot_provider: "byparr" | "camoufox"` اختياري في `SpiderConfig` (افتراضي byparr) + contract tests متوازية للاتنين + `mock_target_camoufox.yaml` وتشغيله فعليًا ضد نفس تحدي Anubis اللي Byparr فشل فيه - نتيجة الكود: نجح كامل (بعد 3 إصلاحات حقيقية متتالية في نفس الجولة: (1) crash فوري بسبب استدعاء Playwright's sync API على reactor thread بتاع Scrapy's asyncio - اتصلح بـ deferToThread زي PlaywrightMiddleware بالظبط؛ (2) `COOKIE_PARTITIONED=false` كان اتّحقّق منه يدويًا في round 2 بس ما اتضافش فعليًا لـ docker-compose.test.yml - غلطة حقيقية اتصلحت؛ بعد الاتنين، Camoufox عدّى تحدي Anubis الحقيقي بالكامل ورجّع posts حقيقية - CI run 32507637737، 21/21 اختبار PASSED) - القرار: نصلح دلوقتي (تفاصيل كاملة + "Antibot Provider Comparison" الحقيقية بين Byparr وCamoufox في `docs/REQUIREMENTS.md` قسم 9؛ فجوة Byparr نفسه upstream لسه مسجّلة ومفتوحة، مقترح issue مكتوب بس مش مرفوع بسبب قيد صلاحيات الـ session)

[2026-08-21] من مستوى 1 لمستوى 2 - أضفنا (1) `byparr` instance مخصص جوه `docker-compose.test.yml` بـ `network_mode: "service:anubis"` (يشارك network namespace بتاع Anubis حرفيًا، بدل ما يفضل `services:` container منفصل على شبكة تانية زي الجولة اللي فاتت) و(2) `COOKIE_SECURE=false` + `COOKIE_PARTITIONED=false` على Anubis (flags حقيقية موثّقة في كوده المصدري، مش TLS — Anubis مفهوش HTTPS server مدمج أصلاً) - نتيجة الكود: فشل جزئي (0 items لسه - بس دلوقتي Byparr فعليًا بيوصل لـ Anubis ويستلم تحدي proof-of-work حقيقي كل مرة (اتّحقّق منه محليًا: لوج Anubis `"new challenge issued"`, weight=10)، مش زي الجولة اللي فاتت اللي مكانش بيوصل أصلاً؛ فجوة تالتة جديدة اكتشفت: Byparr's API بيقفل الـ browser context فور ما `load` event يحصل، قبل ما JS الـ challenge الـ async يشتغل خالص - اتّحقّق منه محليًا بمراقبة لوج Anubis لأكتر من 20 ثانية من غير أي نشاط تاني؛ الاختبارين الحيين اتأكّدوا PASSED فعليًا في CI run 32500347313، والـ stack (mock-target + anubis + byparr المخصص) اتبنى واتشغّل بنجاح فيه (byparr healthy خلال ~14 ثانية)) - القرار: نأجل ونسجل (تفاصيل كاملة للفجوة الجديدة في `docs/REQUIREMENTS.md` قسم 9 بند 4 - تحتاج كود جديد في `ByparrProvider` أو تعديل upstream في Byparr نفسه، مش تعديل سريع)

[2026-08-21] من مستوى 0 لمستوى 1 - أضفنا mock-target خلف Anubis (PoW الحقيقي، policy افتراضية أصلية) + BotD (log-only) + 4 تحديات هيكلية (markup randomizer، honeypots، decoy data، /feed) + `src/spiders/configs/mock_target.yaml` وتشغيله فعليًا ضد الـ stack الكامل في CI (run 32479883962) - نتيجة الكود: فشل جزئي (0 items - في CI الحقيقي، Byparr's services: container مقدرش يوصل لـ Anubis خالص بسبب اختلاف شبكة Docker، فالطلب رجع لـ plain Scrapy اترفض صراحة من Anubis؛ فجوة تانية مستقلة اتأكدت محليًا: حتى لو Byparr وصل فعلاً، تحدي Anubis مقدرش يكتمل فوق HTTP عادي بسبب Secure cookie؛ الكراول خلص من غير crash، مفيش honeypot اتلمس) - القرار: نأجل ونسجل (تفاصيل كاملة للفجوتين + إصلاح جانبي حقيقي اتنفذ فورًا لـ `TITAN_BYPARR_URL` env fallback في `docs/REQUIREMENTS.md` قسم 9)
