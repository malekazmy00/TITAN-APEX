"""Abstract contracts (interfaces) for pluggable providers.

Any concrete implementation of these interfaces must also pass the
matching suite in ``tests/contract/`` before it is accepted into the
project (see docs/REQUIREMENTS.md, section 1 & 4).
"""
